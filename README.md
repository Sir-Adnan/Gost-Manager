
# 🚀 Gost v3 Manager (Igost Edition)

<p align="center">
  <img src="https://img.shields.io/badge/Core-Gost_v3-00ADD8?style=flat-square&logo=go">
  <img src="https://img.shields.io/badge/Language-Bash-4EAA25?style=flat-square&logo=gnu-bash">
  <img src="https://img.shields.io/github/license/Sir-Adnan/Gost-Manager?style=flat-square">
  <br>
  <b>The Ultimate Assistant for Managing Gost Tunnels on Linux</b>
</p>

این اسکریپت یک ابزار مدیریت قدرتمند و تعاملی برای **Gost v3** است که فرآیند ساخت تونل‌های ساده، امن و لودبالانسینگ را به شدت ساده می‌کند. با رابط کاربری مدرن (Adaptive UI) و پشتیبانی کامل از IPv6، دیگر نیازی به درگیری با فایل‌های کانفیگ پیچیده YAML ندارید.

## 🖼️ تصاویر محیط اسکریپت (Screenshots)

<p align="center">
  <img src="/images/main.png" width="45%" alt="Menu">
</p>

## ✨ ویژگی‌های کلیدی (Features)

* 🎨 **رابط کاربری تطبیق‌پذیر (Adaptive UI):** طراحی شده برای خوانایی کامل در هر دو تم روشن (Light) و تاریک (Dark) ترمینال.
* 🚀 **شورت‌کات `igost`:** پس از اولین نصب، کافیست فقط کلمه `igost` را تایپ کنید تا پنل باز شود.
* 🔒 **تانل‌های امن (Secure Tunnels):** راه‌اندازی آسان **WSS**, **gRPC**, **QUIC** با تولید خودکار گواهی‌نامه SSL (Self-Signed).
* 🌐 **پشتیبانی Dual Stack:** تشخیص هوشمند و اصلاح فرمت **IPv6** (تزریق خودکار براکت `[]`).
* ⚖️ **لود بالانسر پیشرفته:** پشتیبانی از استراتژی‌های Round Robin, Random, Least Conn و Hashing.
* 🛡️ **امنیت و پایداری:**
    * بررسی تداخل پورت‌ها قبل از ساخت سرویس.
    * سیستم پشتیبان‌گیری خودکار (`config.yaml.bak`) قبل از هر تغییر.
    * استفاده از پردازنده‌ی قدرتمند `yq` برای ویرایش استاندارد فایل YAML.
* 📊 **داشبورد زنده:** نمایش وضعیت CPU، RAM، ترافیک شبکه (vnstat) و وضعیت سرویس.

---

## 📥 نصب و راه‌اندازی (Installation)

برای نصب، دستور زیر را در سرور خود (Ubuntu/Debian) اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Sir-Adnan/Gost-Manager/main/gost.sh)
```

### ⚡ دسترسی سریع با `igost`

پس از اولین اجرا، اسکریپت از شما می‌پرسد که آیا مایل به نصب میانبر هستید؟ با تایید آن، در دفعات بعدی فقط کافیست دستور زیر را بزنید:

```bash
igost

```

---

## 📚 راهنمای گزینه‌ها (Menu Guide)

در اینجا توضیح می‌دهیم هر گزینه در منو چه کاری انجام می‌دهد:

### `[1]` 🚀 Direct Tunnel (تانل مستقیم)

برای انتقال ترافیک ساده از یک پورت سرور به یک مقصد دیگر.

* **کاربرد:** پورت فورواردینگ ساده.
* **قابلیت‌ها:** پشتیبانی از TCP، UDP و یا هر دو به صورت همزمان (Dual Stack).

### `[2]` 🔒 Secure Tunnel (تانل رمزنگاری شده)

مهم‌ترین بخش برای مخفی‌سازی ترافیک و عبور از فایروال‌ها. دارای دو حالت:

1. **Sender (Client):** سروری که ترافیک را می‌گیرد، رمزنگاری می‌کند و به سرور خارج می‌فرستد (مناسب سرور ایران).
* *قابلیت:* تنظیم SNI دلخواه برای دور زدن فیلترینگ.


2. **Receiver (Server):** سروری که ترافیک رمز شده را دریافت کرده و رمزگشایی می‌کند (مناسب سرور خارج).
* *قابلیت:* تولید خودکار گواهی SSL معتبر برای ارتباط داخلی.



### `[3]` ⚖️ Load Balancer (توزیع بار)

پخش کردن ترافیک ورودی بین چندین سرور مقصد.

* **استراتژی‌ها:**
* `Round Robin`: نوبتی (یکی به من، یکی به تو).
* `Random`: انتخاب تصادفی.
* `Least Conn`: ارسال به سروری که کمترین اتصال فعال را دارد (هوشمند).
* `Hashing`: کاربر ثابت همیشه به یک سرور خاص وصل می‌شود (Sticky IP).


* **تست اتصال:** قبل از افزودن نود، اسکریپت در دسترس بودن IP مقصد را چک می‌کند.

### `[4]` 🗑️ Delete Service (حذف سرویس)

نمایش لیست سرویس‌های فعال به صورت جدول مرتب و امکان حذف آن‌ها بر اساس ID.

### `[5]` 🔧 Edit Config

باز کردن فایل `config.yaml` با ویرایشگر `nano` برای تغییرات دستی پیشرفته. (همراه با بک‌آپ اتوماتیک).

### `[6]` 📊 View Logs

نمایش لاگ‌های زنده سرویس Gost برای عیب‌یابی و بررسی اتصالات.

### `[7]` 🚪 Uninstall

حذف کامل سرویس Gost، فایل‌های کانفیگ، میانبر `igost` و تمیزسازی سیستم.

---

## 🛠️ پیش‌نیازها (Requirements)

* سیستم عامل: Ubuntu 20.04+ یا Debian 10+
* دسترسی روت (Root Access)
* پورت‌های آزاد برای سرویس‌ها

---

<p align="center">
Created with ❤️ by <b>UnknownZero</b>
</p>
