# 🚀 Gost v3 Manager (Ultimate Silent Edition) - v9.3.4

<p align="center">
  <pre align="center">
   ██████╗  ██████╗ ███████╗████████╗
  ██╔════╝ ██╔═══██╗██╔════╝╚══██╔══╝
  ██║  ███╗██║   ██║███████╗   ██║   
  ██║   ██║██║   ██║╚════██║   ██║   
  ╚██████╔╝╚██████╔╝███████║   ██║   
   ╚═════╝  ╚═════╝ ╚══════╝   ╚═╝   
  <b>M  A  N  A  G  E  R    v 9 . 3 . 4</b>
  </pre>
</p>

<p align="center">
  ◈ <b>CORE:</b> <code>Gost v3</code> ◈ <b>VERSION:</b> <code>9.3.4</code> ◈ <b>STATUS:</b> <code>High Performance</code> ◈
</p>

<p align="center">
  <img src="https://img.shields.io/badge/🛡️_Security-Maximum-blue?style=flat-square">
  <img src="https://img.shields.io/badge/⚡_Speed-Optimized-green?style=flat-square">
  <img src="https://img.shields.io/badge/💎_Stability-Ultra-brightgreen?style=flat-square">
</p>

---




<b>پیشرفته‌ترین و پایدارترین دستیار مدیریت تانل‌های Gost در لینوکس</b>
</p>

این اسکریپت نسخه تکامل‌یافته مدیریت Gost است که مخصوص **ترافیک‌های سنگین (هزاران کاربر)** و **سرورهای با رم بالا (۴ تا ۱۶ گیگ)** بهینه شده است. در این نسخه، مشکل پر شدن هارد سرور به دلیل لاگ‌های اضافی به طور کامل ریشه‌کن شده است.

---

## ✨ ویژگی‌های برجسته در این نسخه (What's New)

* 💎 **سکوت مطلق (Total Silence):** در حالت عادی هیچ لاگی روی دیسک نوشته نمی‌شود تا عمر هارد افزایش یابد و سرور پر نشود.
* 🚀 **پرفورمنس طلایی (GOGC=100):** تنظیمات حافظه برای استفاده حداکثری از رم و کاهش فشار روی CPU بهینه شده است.
* 🛡️ **واچ‌داگ هوشمند (Light Watchdog):** سیستم پایش خودکار که اگر لود سرور غیرطبیعی شود، سرویس را ریستارت می‌کند تا از هنگ کامل جلوگیری شود.
* 📦 **پشتیبانی کامل از IPv6 و Dual Stack:** تانل‌زنی راحت روی بسترهای مختلف شبکه.
* 📊 **داشبورد هوشمند:** نمایش لحظه‌ای مصرف منابع و تعداد تانل‌های فعال.

---

## 📥 نصب و اجرا (Installation)

برای نصب یا آپدیت به آخرین نسخه، دستور زیر را اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Sir-Adnan/Gost-Manager/main/gost.sh)

```

## 📥 نصب نسخه بتا (BETA)

برای نصب یا آپدیت به آخرین نسخه، دستور زیر را اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Sir-Adnan/Gost-Manager/main/gost-beta.sh)

```
### ⚡ دسترسی سریع

پس از نصب، با تایپ کلمه زیر در هر جای ترمینال، پنل مدیریت باز می‌شود:

```bash
igost

```

---



## 📥 نصب دستی
برای اینکه این اسکریپت و فایل‌های مورد نیازش را بدون استفاده از دستورات خودکار (Direct Install) و به صورت دستی روی سرور اوبونتو نصب و مدیریت کنید، باید مراحل زیر را دنبال کنید.

این کار معمولاً زمانی انجام می‌شود که می‌خواهید روی محتوای فایل‌ها نظارت داشته باشید یا سرور شما دسترسی مستقیم به اینترنت برای دانلود خودکار ندارد.

### مرحله ۱: ذخیره و انتقال اسکریپت اصلی

۱. محتویاتی که در بالا فرستادید را در کامپیوتر خود در یک فایل متنی با نام `gost.sh` ذخیره کنید.
۲. با استفاده از یک نرم‌افزار انتقال فایل (مثل **WinSCP** یا **FileZilla**) یا دستور `scp` در ترمینال، این فایل را به سرور منتقل کنید:

```bash
scp path/to/gost.sh root@YOUR_SERVER_IP:/root/

```

---

### مرحله ۲: نصب پیش‌نیازهای سیستمی (Dependencies)

اسکریپت برای اجرا به تعدادی ابزار نیاز دارد. ابتدا این دستور را در سرور بزنید تا ابزارهای پایه نصب شوند:

```bash
apt update && apt install -y curl openssl lsof nano netcat-openbsd vnstat logrotate cron

```

---

### مرحله ۳: دانلود و جایگذاری دستی فایل‌های باینری (مهم)

اسکریپت شما به دو فایل اجرایی اصلی وابسته است: **Gost** و **yq**. اگر نمی‌خواهید اسکریپت خودش آن‌ها را دانلود کند، باید دستی این کار را بکنید:

#### الف) نصب باینری yq (برای مدیریت فایل‌های YAML):

۱. به [صفحه ریلیزهای yq](https://www.google.com/search?q=https://github.com/mikefarah/yq/releases/latest) بروید.
۲. نسخه مناسب معماری سرور خود را دانلود کنید (معمولاً `yq_linux_amd64`).
۳. نام آن را به `yq` تغییر دهید، به مسیر `/usr/bin/` منتقل کنید و دسترسی اجرایی بدهید:

```bash
mv yq_linux_amd64 /usr/bin/yq
chmod +x /usr/bin/yq

```

#### ب) نصب باینری Gost:

۱. به [صفحه ریلیزهای Gost](https://github.com/go-gost/gost/releases) بروید.
۲. فایل `gost_linux_amd64.tar.gz` را دانلود و استخراج کنید.
۳. فایل اجرایی `gost` را به مسیر `/usr/bin/` منتقل کرده و دسترسی بدهید:

```bash
mv gost /usr/bin/gost
chmod +x /usr/bin/gost

```

---

### مرحله ۴: ایجاد پوشه‌ها و فایل پیکربندی

طبق کدهای اسکریپت، مسیرهای زیر باید ساخته شوند تا اسکریپت دچار خطا نشود:

```bash
mkdir -p /etc/gost/certs
chmod 700 /etc/gost/certs
echo "services: []" > /etc/gost/config.yaml

```

---

### مرحله ۵: آماده‌سازی و اجرای اسکریپت

حالا که فایل `gost.sh` را به سرور فرستادید، به پوشه‌ای که فایل در آن است بروید (مثلاً `/root`) و مراحل زیر را انجام دهید:

۱. **اجازه اجرا دادن به اسکریپت:**

```bash
chmod +x gost.sh

```

۲. **ایجاد میانبر (Shortcut) - اختیاری:**
اگر می‌خواهید با تایپ کلمه `igost` اسکریپت باز شود، فایل را به مسیر bin کپی کنید:

```bash
cp gost.sh /usr/local/bin/igost
chmod +x /usr/local/bin/igost

```

۳. **اجرای نهایی:**

```bash
./gost.sh
```
# یا اگر میانبر ساختید:
```bash
igost

```

---

### نکات بسیار مهم برای پایداری:

* **سرویس Systemd:** اسکریپت شما در اولین اجرا سعی می‌کند فایلی در مسیر `/etc/systemd/system/gost.service` بسازد. اگر این کار را نکرد، محتویات بخش `create_service` در اسکریپت را بردارید و دستی در آن مسیر ذخیره کنید.
* **Logrotate:** برای جلوگیری از پر شدن حافظه توسط لاگ‌ها، حتماً مطمئن شوید که فایل `/etc/logrotate.d/gost-watchdog` طبق کدهای اسکریپت ایجاد شده باشد.
* **امنیت:** اگر از تونل‌های ایمن (Secure Tunnel) استفاده می‌کنید، اسکریپت خودش گواهینامه‌های SSL را در `/etc/gost/certs` می‌سازد؛ پس نیازی به کپی دستی گواهینامه ندارید مگر اینکه بخواهید از گواهینامه شخصی خودتان استفاده کنید.



## 📚 راهنمای جامع گزینه‌های منو

اگر برای اولین بار است که از اسکریپت استفاده می‌کنید، این راهنما به شما کمک می‌کند:

### `[1]` 🚀 Direct Tunnel (تانل مستقیم)

سریع‌ترین راه برای انتقال ترافیک.

* **کاربرد:** پورت فورواردینگ ساده از ایران به خارج.
* **پروتکل مناسب:** برای اکثر کاربران **TCP+UDP (Dual Stack)** بهترین گزینه است.
* **mTCP:** اگر می‌خواهید سرعت را افزایش دهید، این گزینه با مالتی‌پلکس کردن کانکشن‌ها سرعت بهتری می‌دهد.

### `[2]` 🔒 Secure Tunnel (تانل امن)

برای عبور از فایروال‌های سخت‌گیر.

* **Sender (Client):** روی سرور **ایران** نصب می‌شود.
* **Receiver (Server):** روی سرور **خارج** نصب می‌شود.
* **بهترین ترکیب:** استفاده از پروتکل **mWSS** یا **H2** به همراه یک SNI معتبر (مثل `google.com` یا `microsoft.com`).

### `[3]` ⚖️ Load Balancer (توزیع بار)

اگر چندین سرور خارج دارید، ترافیک را بین آن‌ها پخش کنید.

* **استراتژی پیشنهادی:** برای هزاران کاربر، حتماً از **`Least Conn`** استفاده کنید تا کاربران به خلوت‌ترین سرور متصل شوند.

### `[4]` 🔌 Simple Proxy (پراکسی ساده)

ساخت پراکسی SOCKS5 یا HTTP.

* **کاربرد:** استفاده مستقیم در تلگرام یا مرورگر.
* **نکته:** حتماً برای امنیت خودتان، یوزرنیم و پسورد قوی ست کنید.

### `[5]` 🛡️ Secure DNS

راه اندازی DNS اختصاصی روی سرور (DoH).

* **کاربرد:** جلوگیری از نشت DNS و افزایش امنیت در اتصال.

### `[8]` 📊 Logs (مدیریت لاگ و دیسک) - **بخش حیاتی**

در این بخش می‌توانید وضعیت سلامت سرور را بررسی کنید:

* **Toggle Debug Mode:** اگر تانل شما وصل نمی‌شود، دیباگ را روشن کنید تا علت را ببینید. پس از رفع مشکل، حتماً دوباره آن را خاموش کنید تا دیسک پر نشود.
* **Vacuum Journal:** با این گزینه می‌توانید لاگ‌های قدیمی سیستم را که فضا اشغال کرده‌اند (مثلاً ۵۰۰ مگابایت) به حجم کمی (مثل ۱۰۰ مگابایت) کاهش دهید.
* **Truncate Syslog:** خالی کردن فوری فایل syslog که معمولاً در حملات SSH حجیم می‌شود.

### `[9]` 🔄 Auto-Restart (Watchdog)

فرشته نجات سرور شما.

* **مقدار پیشنهادی:** این مقدار باید بر اساس تعداد هسته‌های CPU شما باشد. فرمول پیشنهادی: **(تعداد هسته × ۲)**.
* **مثال:** اگر سرور شما ۸ هسته دارد، عدد را روی **۱۶** بگذارید.

---

## 💡 مقادیر پیشنهادی برای بهترین بازدهی

| سخت‌افزار سرور | مقدار GOGC | وضعیت لاگ | مقدار Watchdog |
| --- | --- | --- | --- |
| رم زیر ۱ گیگ | 20 | Silent | 4 |
| رم ۲ تا ۴ گیگ | 80 | Silent | 8 |
| رم ۴ تا ۱۶ گیگ | **100** | **Silent** | **16 (یا ۲ برابر هسته)** |

---

## 🛠️ عیب‌یابی سریع (Troubleshooting)

1. **سرویس وضعیت ACTIVE دارد اما تانل کار نمی‌کند؟**
* احتمالا پورت در فایروال سرور (UFW) یا پنل دیتاسنتر بسته است.


2. **سرور ایران به خارج وصل نمی‌شود؟**
* از منوی ۸ دیباگ را روشن کنید. اگر ارور `TLS handshake timeout` دیدید، یعنی SNI یا پروتکل شما توسط زیرساخت شناسایی و محدود شده است. از پروتکل‌های امن‌تر مثل `WSS` استفاده کنید.



---

<p align="center">
Created with ❤️ by <b>UnknownZero</b>


---
